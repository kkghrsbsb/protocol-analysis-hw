# 作业1 要求（原述）：
1. （可选）安装win7/win10/64位虚拟机，在虚拟机/本机中安装Wireshark（Wireshark是一个监控软件，安装的服务和后台脚本比较多，推荐安装到虚拟机里）。
2. 自行使用Wireshark进行抓包，分别通过TCP/IP/ICMP和address进行过滤，抓取不同类型的报文/帧数据，并通过协议解析，提取中间的所有字段，写到表格里。（可以借助大模型生成IP转二进制、二进制转10进制，16进制等工具辅助做题）
3. 将上述试验过程写入下面报告，并完成总结与收获
4. 将产出的代码和作品打包上传，后面会展示优秀作品